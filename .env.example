# Same-origin by default so Vite/`npm start` can serve Keterbukaan and
# Fundamentals from the local disclosure fixture. Do not point this at :8787
# unless that origin also serves the disclosure allowlist.
# VITE_API_BASE=http://localhost:8787
