# Private backend origin. Do not include a trailing slash or expose it publicly.
VITE_API_BASE=http://localhost:8787
# Enable only when this build talks directly to the private Tailscale API.
VITE_PRIVATE_WRITES=false
